# Example EnvironmentFile for production (do NOT commit with real secrets)
# Place this file on the VPS at /etc/skytrack/env and protect permissions (chmod 600)

DATABASE_URL_BACKEND=postgresql://<DB_USER>:<DB_PASS>@your-rds-endpoint.region.rds.amazonaws.com:5432/skytrack_db?connect_timeout=300
DATABASE_URL_BROKER=postgresql://<DB_USER>:<DB_PASS>@your-rds-endpoint.region.rds.amazonaws.com:5432/skytrack_db?connect_timeout=300
POSTGRES_SSL=require
MQTT_USERNAME=<MQTT_USER>
MQTT_PASSWORD=<MQTT_PASS>
VITE_API_URL=http://<FRONTEND_HOST>:5173
